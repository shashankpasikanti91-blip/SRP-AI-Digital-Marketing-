# ── SRP Marketing OS — Development Overrides ────────────────────────────────
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up

services:

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: uvicorn app.main:create_app --factory --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./backend:/app  # writable for hot-reload
    environment:
      ENVIRONMENT: development
      DEBUG: "true"
      REDIS_URL: "redis://:redis_secret@redis:6379/0"
      DATABASE_URL: "postgresql+asyncpg://ats_user:ats_password@db:5432/srp_marketing"
      CELERY_BROKER_URL: "redis://:redis_secret@redis:6379/0"
      CELERY_RESULT_BACKEND: "redis://:redis_secret@redis:6379/1"
    ports:
      - "8001:8000"

  celery-worker:
    command: celery -A app.workers.celery_app worker --loglevel=debug --concurrency=2 --queues=celery,ai,social,email
    environment:
      REDIS_URL: "redis://:redis_secret@redis:6379/0"
      DATABASE_URL: "postgresql+asyncpg://ats_user:ats_password@db:5432/srp_marketing"
      CELERY_BROKER_URL: "redis://:redis_secret@redis:6379/0"
      CELERY_RESULT_BACKEND: "redis://:redis_secret@redis:6379/1"
    volumes:
      - ./backend:/app

  celery-beat:
    command: celery -A app.workers.celery_app beat --loglevel=debug --schedule /tmp/celerybeat-schedule
    environment:
      REDIS_URL: "redis://:redis_secret@redis:6379/0"
      DATABASE_URL: "postgresql+asyncpg://ats_user:ats_password@db:5432/srp_marketing"
      CELERY_BROKER_URL: "redis://:redis_secret@redis:6379/0"
      CELERY_RESULT_BACKEND: "redis://:redis_secret@redis:6379/1"
    volumes:
      - ./backend:/app

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    environment:
      VITE_API_URL: http://localhost:8001/api/v1

  # Flower — Celery monitoring UI (dev only)
  flower:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: celery -A app.workers.celery_app flower --port=5555
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-srp}:${POSTGRES_PASSWORD:-srp_secret}@db:5432/${POSTGRES_DB:-srp_marketing}
      REDIS_URL: redis://:${REDIS_PASSWORD:-redis_secret}@redis:6379/0
    ports:
      - "5555:5555"
    depends_on:
      - redis
    networks:
      - backend
